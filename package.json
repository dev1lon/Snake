{
  "name": "snake-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm test --prefix backend && node backend/node_modules/tsx/dist/cli.mjs --test frontend/test/pendingRuns.test.ts",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "lint": "npm run lint --prefix frontend && npm run lint --prefix backend",
    "install:all": "npm install",
    "postinstall": "npm install --include=dev --prefix backend && npm install --include=dev --prefix frontend",
    "render:backend": "npm install --include=dev --prefix backend && npm run build --prefix backend",
    "render:frontend": "npm install --include=dev --prefix frontend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.8.3"
  }
}
